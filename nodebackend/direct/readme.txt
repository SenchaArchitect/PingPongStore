Place your ExtDirect classes here.
File name must match exports name.

For example:

var DXFormTest = {
    testMe: function(params, callback) {
        callback({
            msg: 'Hello world',
            params: params
        });
    }
};

module.exports = DXFormTest;